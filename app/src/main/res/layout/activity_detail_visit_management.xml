<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".features.features_management.homescreen.visitTracking.ui.activity.DetailVisitManagementActivity">

    <include
        android:id="@+id/appbarDetailVisitManagement"
        layout="@layout/app_bar_default"/>

    <LinearLayout
        android:id="@+id/clDetailVisitManagement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_20sdp"
        android:layout_marginHorizontal="@dimen/_20sdp"
        android:paddingVertical="@dimen/_8sdp"
        android:paddingHorizontal="@dimen/_10sdp"
        android:background="@drawable/bg_field"
        android:weightSum="1.7"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/appbarDetailVisitManagement">

        <TextView
            android:id="@+id/tvDateDetailVisitManagement"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.7"
            android:text="Error date"
            android:textAlignment="textStart"
            android:textColor="@color/neutral100"
            android:textSize="@dimen/_12ssp"
            android:fontFamily="@font/poppinsmedium"
            android:maxLines="1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tvCountProjectDetailVisitManagement"/>

        <TextView
            android:id="@+id/tvCountProjectDetailVisitManagement"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/_4sdp"
            android:text="error"
            android:textAlignment="textEnd"
            android:textColor="@color/primary_color"
            android:textSize="@dimen/_11ssp"
            android:maxLines="1"
            android:fontFamily="@font/poppinsmedium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/tvDateDetailVisitManagement"/>

    </LinearLayout>

    <TextView
        android:id="@+id/tvDetailVisitManagement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_14sdp"
        android:layout_marginHorizontal="@dimen/_20sdp"
        android:text="Laporan Kunjungan"
        android:textColor="@color/grayTxt"
        android:textSize="@dimen/_14ssp"
        android:fontFamily="@font/poppinsmedium"
        app:layout_constraintTop_toBottomOf="@id/clDetailVisitManagement"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvHistoryVisitManagement"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="@dimen/_20sdp"
        android:layout_marginTop="@dimen/_8sdp"
        android:visibility="visible"
        tools:listitem="@layout/list_tracking_project"
        app:layout_constraintTop_toBottomOf="@id/tvDetailVisitManagement"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/tvEmptyHistoryVisitManagement"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="@dimen/_20sdp"
        android:layout_marginBottom="@dimen/_20sdp"
        android:gravity="center"
        android:text="Tidak ada catatan kunjungan project pada tanggal ini"
        android:textColor="@color/grayTxt"
        android:textSize="@dimen/_12ssp"
        android:fontFamily="@font/poppinsregular"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tvDetailVisitManagement"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>